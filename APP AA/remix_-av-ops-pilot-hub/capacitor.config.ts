import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agendaaeronautas.app',
  appName: 'Agenda Aeronautas',
  webDir: 'dist'
};

export default config;
